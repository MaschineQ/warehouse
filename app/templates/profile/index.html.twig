{% extends 'base.html.twig' %}

{% block title %}{{ 'Profile'|trans }}{% endblock %}

{% block bread %}
    → {{ 'Profile'|trans }}
{% endblock %}

{% block body %}
    <div class="w-full">
        <div class="bg-white border rounded shadow">
            <div class="border-b p-3">
                <h5 class="font-bold uppercase text-gray-600">{{ 'Profile'|trans }}</h5>
            </div>
            <div class="p-5">
                {{ include('profile/_profile_form.html.twig', ) }}
            </div>
        </div>
    </div>
{% endblock %}
